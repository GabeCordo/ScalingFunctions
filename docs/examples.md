# ScalingFunctions Examples

Examples demonstrate different use-cases for developers looking to leverage the framework.

## Three Consumers Example
The simplest example demonstrating how a developer can build a pipeline with a set of pure functions. Developers should opt-into
this example when using the framework for the first time.

Details about the three consumer example can be found [here](../examples/three-consumers/README.md).